# ============================================================
# HiFi-ESP32-Plus - Snapclient Configuration
# ============================================================
# ESP32 audio board with PCM5122 DAC, I2S output, with entry-level DSP
# Configured as a Snapcast client for synchronized audio
# There are two implementations available:
# 1) Basic snapclient without DSP controls (c-MM original port)
# 2) snapclient with added DSP controls (farmed-switch fork of c-MM port)
#     Frequencies: 40, 50, 60, 70, 80, 90, 100, 110, 120, 130,
#                 140, 200, 315, 500, 800, 1250, 2000, 5000 Hz
#     Range: -15dB to +15dB per band
# Board-specific pinouts defined in substitutions
# Functional blocks imported from /packages
# ============================================================

substitutions:
  name: esphome-web-02b6ac
  friendly_name: HIFI-ESP32-Plus
  task_stack_in_psram: "false"

  # ===== Board-Specific Pinouts =====
  # I2S Audio (PCM5100 DAC)
  i2s_lrclk_pin: GPIO25
  i2s_bclk_pin: GPIO26
  i2s_dout_pin: GPIO22
  
  # I2C pins for DAC control
  i2c_sda_pin: GPIO21               # I2C pins are swapped on the rev J. 
  i2c_scl_pin: GPIO27               # These are correct for Rev J1 and later
  pcm5122_i2c_address: 0x4D         
  pcm5122_enable_pin: GPIO13        # optional, to enable/disable the DAC (XSMT physical pin)

  # RGB LED
  rgb_led_pin: GPIO12
  
  # IR Receiver
  ir_rx_pin: GPIO39

  # Ethernet (W5500)
  eth_cs_pin: GPIO05
  eth_interrupt_pin: GPIO35
  eth_reset_pin: GPIO14

  # SPI pins
  spi_clk_pin: GPIO18
  spi_mosi_pin: GPIO23
  spi_miso_pin: GPIO19
  
  # Display control pins
  display_cs_pin: GPIO15
  display_dc_pin: GPIO04
  display_reset_pin: GPIO32

  # ===== Snapcast Server Configuration =====
  snapcast_server_ip: "192.168.1.49"
  snapcast_server_port: "1704"

esphome:
  name: ${name}
  friendly_name: ${friendly_name}
  min_version: 2025.12.2
  platformio_options:
    board_build.flash_mode: dio

esp32:
  variant: esp32
  flash_size: 8MB
  cpu_frequency: 240MHz
  framework:
    type: esp-idf
    version: recommended
    advanced:
      minimum_chip_revision: "3.1" 
    sdkconfig_options:
      # Moves instructions and read only data from flash into PSRAM on boot.
      # Both enabled allows instructions to execute while a flash operation is in progress without needing to be placed in IRAM.
      # Considerably speeds up mWW at the cost of using more PSRAM.
      CONFIG_SPIRAM_RODATA: "y"
      CONFIG_SPIRAM_FETCH_INSTRUCTIONS: "y"
      CONFIG_BT_ALLOCATION_FROM_SPIRAM_FIRST: "y"
      CONFIG_BT_BLE_DYNAMIC_ENV_MEMORY: "y"
      CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC: "y"
      CONFIG_MBEDTLS_SSL_PROTO_TLS1_3: "y"

logger:
  level: DEBUG

api:

ota:
  platform: esphome
  password: !secret esphome_ota_password

wifi:
  ssid: !secret esphome_wifi_ssid
  password: !secret esphome_wifi_password

psram:
  speed: 80MHz

network:
  enable_ipv6: true

# ===== Packages =====
packages:
  esparagus_remote:
    url: https://github.com/sonocotta/esp32-audio-dock
    ref: main
    files:
      - firmware/esphome/packages/light.yaml
      - firmware/esphome/packages/dac-pcm5122.yaml
      - firmware/esphome/packages/ir-receiver.yaml
      - firmware/esphome/packages/monitoring.yaml
      - firmware/esphome/packages/monitoring-wifi.yaml
      - firmware/esphome/packages/oled.yaml
      # Option A: @c-MM original port of esp32-snapclient client
      # Basic implementation without DSP controls
      # - firmware/esphome/packages/snapclient.yaml
      # Option B: @farmed-switch fork of the c-MM snapclient with added DSP controls
      # Implements software BQ-filters, but makes less sense for DSP-enabled DACs
      # - firmware/esphome/packages/snapclient-with-dsp.yaml
      
      # Optional ethernet (replace wifi config if you use this, you need to exclude OLED package if you do this to free up SPI bus)
      # - firmware/esphome/packages/ethernet-w5500.yaml

  # # Debug/local development fallback:
  # light: !include packages/light.yaml
  # pcm5122_dac: !include packages/dac-pcm5122.yaml
  # ir_receiver: !include packages/ir-receiver.yaml
  # monitoring: !include packages/monitoring.yaml
  # monitoring_wifi: !include packages/monitoring-wifi.yaml
  # oled: !include packages/oled.yaml
  snapclient: !include packages/snapclient-new.yaml
  external_dac_addon: !include packages/snapclient-addon-external-dac.yaml
  dac_enable: !include packages/snapclient-addon-dac-enable.yaml
  # snapclient_with_dsp: !include packages/snapclient-with-dsp.yaml
  # # ethernet: !include packages/ethernet-w5500.yaml
